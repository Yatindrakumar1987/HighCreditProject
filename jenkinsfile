pipeline {
     agent any

     stages {
        stage('Build') {
            steps {
                echo "build successfully completed"
            }
        }

        stage('Test') {
            steps {
                echo "complete test completed successfully"
            }
        }

        stage('Package') {
            steps {
                echo "package completed successfully"
            }
        }

        stage('Deploy') {
            steps {
                echo "deploy completed successfully"
            }
        }
    }
 }
